

# FlowSilhouette: Blue-tinted borders + Right arm repositioning

## Changes in `src/components/FlowSilhouette.tsx`

### 1. Ellipse styling (rendering block)
- Change unselected `stroke` from `rgba(255,255,255,0.30)` → `rgba(46,134,171,0.25)`
- Change unselected `fill` from `rgba(255,255,255,0.06)` → `rgba(46,134,171,0.04)`

### 2. `AREA_ELLIPSES` array — `braco_dir` entry
- Change `cx: 82` → `cx: 78`, `rx: 10` → `rx: 9`

No other files modified.

